input { 
	udp {
		port  => 5000
		codec => json
	}
} 

output { 
	stdout { codec => rubydebug } 
	file { path => "/log-dir/%{host}/%{facility}-%{+YYYY-MM-dd}.log" }
}

#input {
#	udp {
#		port  => 5000
#		codec => json
#	}
#
#	syslog {
#		port => 514
#		tags => "syslog"
#	}
#}

#output {
#	stdout { codec => rubydebug }
#	kafka  { 
#		topic_id => "logs" 
#		bootstrap_servers => "kafka:9092"
#		batch_size => 1
#       }
#	file {
#		path => "/log-dir/%{host}/%{facility}-%{+YYYY-MM-dd}.log"
#	}
#}
